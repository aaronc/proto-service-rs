[package]
name = "proto-service-examples"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
proto-service = { path = "../proto-service" }
prost = "0.14"

[build-dependencies]
proto-service-build = { path = "../proto-service-build" }
prost-build = "0.14"
